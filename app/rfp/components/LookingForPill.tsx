export const LookingForPill = ({ role }) => {
  return (
    <span className="bg-wet-concrete rounded-full px-2 py-1 flex items-center w-fit text-xs uppercase text-marble-white font-bold">
      Looking for {role}s
    </span>
  )
}

export default LookingForPill
