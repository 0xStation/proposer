export const timeModule = {
  Time: [
    // optional time represented as 0
    { name: "startDate", type: "uint256" }, // optional, UNIX timestamp for when this proposal will begin
    { name: "endDate", type: "uint256" }, // optional, UNIX timestamp for when this proposal will conclude
  ],
}
